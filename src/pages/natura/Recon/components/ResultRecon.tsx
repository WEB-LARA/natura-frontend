import { Result } from 'antd';
import React from 'react';

const ResultRecon: React.FC = () => (
  <Result
    status="success"
    title="Successfully Reconciliation Natura"
    subTitle="Reconciliation Sukses, Natura yang sudah di Reconciliation siap dikirim."
  />
);

export default ResultRecon;
